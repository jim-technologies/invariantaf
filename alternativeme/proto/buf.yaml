version: v2
modules:
  - path: .
lint:
  ignore_only:
    RPC_REQUEST_STANDARD_NAME:
      - alternativeme/v1/alternativeme.proto
    RPC_RESPONSE_STANDARD_NAME:
      - alternativeme/v1/alternativeme.proto
